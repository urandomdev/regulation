// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"regulation/internal/ent/item"
	"regulation/internal/ent/synccursor"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// SyncCursor is the model entity for the SyncCursor schema.
type SyncCursor struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// FK to Item
	ItemID uuid.UUID `json:"item_id,omitempty"`
	// Plaid sync cursor for incremental updates
	Cursor string `json:"cursor,omitempty"`
	// Last successful sync timestamp
	LastSyncAt time.Time `json:"last_sync_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SyncCursorQuery when eager-loading is set.
	Edges        SyncCursorEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SyncCursorEdges holds the relations/edges for other nodes in the graph.
type SyncCursorEdges struct {
	// Item holds the value of the item edge.
	Item *Item `json:"item,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ItemOrErr returns the Item value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SyncCursorEdges) ItemOrErr() (*Item, error) {
	if e.Item != nil {
		return e.Item, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: item.Label}
	}
	return nil, &NotLoadedError{edge: "item"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SyncCursor) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case synccursor.FieldCursor:
			values[i] = new(sql.NullString)
		case synccursor.FieldLastSyncAt:
			values[i] = new(sql.NullTime)
		case synccursor.FieldID, synccursor.FieldItemID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SyncCursor fields.
func (_m *SyncCursor) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case synccursor.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case synccursor.FieldItemID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field item_id", values[i])
			} else if value != nil {
				_m.ItemID = *value
			}
		case synccursor.FieldCursor:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field cursor", values[i])
			} else if value.Valid {
				_m.Cursor = value.String
			}
		case synccursor.FieldLastSyncAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_sync_at", values[i])
			} else if value.Valid {
				_m.LastSyncAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SyncCursor.
// This includes values selected through modifiers, order, etc.
func (_m *SyncCursor) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryItem queries the "item" edge of the SyncCursor entity.
func (_m *SyncCursor) QueryItem() *ItemQuery {
	return NewSyncCursorClient(_m.config).QueryItem(_m)
}

// Update returns a builder for updating this SyncCursor.
// Note that you need to call SyncCursor.Unwrap() before calling this method if this SyncCursor
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SyncCursor) Update() *SyncCursorUpdateOne {
	return NewSyncCursorClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SyncCursor entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SyncCursor) Unwrap() *SyncCursor {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SyncCursor is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SyncCursor) String() string {
	var builder strings.Builder
	builder.WriteString("SyncCursor(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("item_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ItemID))
	builder.WriteString(", ")
	builder.WriteString("cursor=")
	builder.WriteString(_m.Cursor)
	builder.WriteString(", ")
	builder.WriteString("last_sync_at=")
	builder.WriteString(_m.LastSyncAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SyncCursors is a parsable slice of SyncCursor.
type SyncCursors []*SyncCursor
